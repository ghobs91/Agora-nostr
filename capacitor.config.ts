import {CapacitorConfig} from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "agorasocial.app",
  appName: "Agora",
  webDir: "dist",
  bundledWebRuntime: false,
}

export default config
